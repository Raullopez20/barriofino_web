---
export interface Props {
    title: string;
    description?: string;
}

const { title, description = "Peña BarrioFino - Fiesta con estilo" } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- AOS CSS -->
    <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
</head>

<body class="bg-white text-gray-900 font-inter antialiased">
<!-- Navigation -->
<nav class="fixed top-0 w-full bg-white/95 backdrop-blur-md z-50 border-b border-gray-200 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16 lg:h-20">
            <!-- Logo -->
            <a href={`${import.meta.env.BASE_URL}`} class="flex items-center space-x-3 group">
                <div class="relative">
                    <div class="w-10 h-10 lg:w-12 lg:h-12 bg-gradient-to-br from-gray-900 to-gray-700 rounded-xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-all duration-300 group-hover:scale-105">
                        <img src={`${import.meta.env.BASE_URL}icono.webp`} alt="Logo BarrioFino" class="w-8 h-8 lg:w-10 lg:h-10 rounded-lg object-cover" />
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-br from-gray-900 to-gray-700 rounded-xl opacity-0 group-hover:opacity-20 blur-md transition-opacity duration-300"></div>
                </div>
                <span class="font-bold text-lg lg:text-xl text-gray-900 group-hover:text-gray-700 transition-colors duration-300">Peña BarrioFino</span>
            </a>

            <!-- Desktop Menu -->
            <div class="hidden lg:flex items-center space-x-1">
                <a href={`${import.meta.env.BASE_URL}`}
                   class={`relative px-4 py-2 rounded-lg font-medium transition-all duration-300 hover:bg-gray-100 ${currentPath === '/' ? 'text-gray-900 bg-gray-50' : 'text-gray-600 hover:text-gray-900'}`}>
                    Inicio
                    {currentPath === '/' && <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-1 h-1 bg-gray-900 rounded-full"></div>}
                </a>
                <a href={`${import.meta.env.BASE_URL}historia`}
                   class={`relative px-4 py-2 rounded-lg font-medium transition-all duration-300 hover:bg-gray-100 ${currentPath === '/historia' ? 'text-gray-900 bg-gray-50' : 'text-gray-600 hover:text-gray-900'}`}>
                    Historia
                    {currentPath === '/historia' && <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-1 h-1 bg-gray-900 rounded-full"></div>}
                </a>
                <a href={`${import.meta.env.BASE_URL}fotos-videos`}
                   class={`relative px-4 py-2 rounded-lg font-medium transition-all duration-300 hover:bg-gray-100 ${currentPath === '/fotos-videos' ? 'text-gray-900 bg-gray-50' : 'text-gray-600 hover:text-gray-900'}`}>
                    Galería
                    {currentPath === '/fotos-videos' && <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-1 h-1 bg-gray-900 rounded-full"></div>}
                </a>
                <a href={`${import.meta.env.BASE_URL}eventos`}
                   class={`relative px-4 py-2 rounded-lg font-medium transition-all duration-300 hover:bg-gray-100 ${currentPath === '/eventos' ? 'text-gray-900 bg-gray-50' : 'text-gray-600 hover:text-gray-900'}`}>
                    Eventos
                    {currentPath === '/eventos' && <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-1 h-1 bg-gray-900 rounded-full"></div>}
                </a>
                <a href={`${import.meta.env.BASE_URL}contacto`}
                   class={`relative px-4 py-2 rounded-lg font-medium transition-all duration-300 hover:bg-gray-100 ${currentPath === '/contacto' ? 'text-gray-900 bg-gray-50' : 'text-gray-600 hover:text-gray-900'}`}>
                    Contacto
                    {currentPath === '/contacto' && <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-1 h-1 bg-gray-900 rounded-full"></div>}
                </a>
            </div>

            <!-- Mobile Menu Button -->
            <button id="mobile-menu-button" class="lg:hidden relative w-10 h-10 rounded-lg hover:bg-gray-100 transition-colors duration-300 flex items-center justify-center group">
                <div class="w-6 h-5 relative flex flex-col justify-between">
                    <span class="mobile-menu-line block h-0.5 bg-gray-900 rounded-full transition-all duration-300 origin-center"></span>
                    <span class="mobile-menu-line block h-0.5 bg-gray-900 rounded-full transition-all duration-300 origin-center"></span>
                    <span class="mobile-menu-line block h-0.5 bg-gray-900 rounded-full transition-all duration-300 origin-center"></span>
                </div>
            </button>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="lg:hidden overflow-hidden max-h-0 transition-all duration-500 ease-in-out">
            <div class="py-6 space-y-2 bg-white border-t border-gray-100">
                <a href={`${import.meta.env.BASE_URL}`}
                   class={`block px-4 py-3 rounded-lg font-medium transition-all duration-300 hover:bg-gray-50 hover:translate-x-1 ${currentPath === '/' ? 'text-gray-900 bg-gray-50' : 'text-gray-600'}`}>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                        </svg>
                        Inicio
                    </div>
                </a>
                <a href={`${import.meta.env.BASE_URL}historia`}
                   class={`block px-4 py-3 rounded-lg font-medium transition-all duration-300 hover:bg-gray-50 hover:translate-x-1 ${currentPath === '/historia' ? 'text-gray-900 bg-gray-50' : 'text-gray-600'}`}>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                        </svg>
                        Historia
                    </div>
                </a>
                <a href={`${import.meta.env.BASE_URL}fotos-videos`}
                   class={`block px-4 py-3 rounded-lg font-medium transition-all duration-300 hover:bg-gray-50 hover:translate-x-1 ${currentPath === '/fotos-videos' ? 'text-gray-900 bg-gray-50' : 'text-gray-600'}`}>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                        </svg>
                        Galería
                    </div>
                </a>
                <a href={`${import.meta.env.BASE_URL}eventos`}
                   class={`block px-4 py-3 rounded-lg font-medium transition-all duration-300 hover:bg-gray-50 hover:translate-x-1 ${currentPath === '/eventos' ? 'text-gray-900 bg-gray-50' : 'text-gray-600'}`}>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                        </svg>
                        Eventos
                    </div>
                </a>
                <a href={`${import.meta.env.BASE_URL}contacto`}
                   class={`block px-4 py-3 rounded-lg font-medium transition-all duration-300 hover:bg-gray-50 hover:translate-x-1 ${currentPath === '/contacto' ? 'text-gray-900 bg-gray-50' : 'text-gray-600'}`}>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                        </svg>
                        Contacto
                    </div>
                </a>
            </div>
        </div>
    </div>
</nav>

<!-- Main Content -->
<main class="pt-16 lg:pt-20 min-h-screen">
    <slot />
</main>

<!-- Footer -->
<footer class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Main Footer Content -->
        <div class="py-12 lg:py-16">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
                <!-- Logo y descripción -->
                <div class="lg:col-span-2">
                    <div class="flex items-center space-x-3 mb-6">
                        <div class="w-12 h-12 bg-gradient-to-br from-gray-100 to-gray-300 rounded-xl flex items-center justify-center shadow-lg">
                            <img src={`${import.meta.env.BASE_URL}icono.webp`} alt="Logo BarrioFino" class="w-10 h-10 rounded-lg object-cover" />
                        </div>
                        <span class="font-bold text-xl text-white">Peña BarrioFino</span>
                    </div>
                    <p class="text-gray-300 text-base leading-relaxed mb-6 max-w-md">
                        Peña de pueblo con tradición y estilo. Llevamos la alegría y la energía a cada rincón de la ciudad, creando momentos inolvidables para toda la comunidad.
                    </p>

                    <!-- Redes Sociales -->
                    <div class="flex items-center space-x-4">
                        <span class="text-sm font-medium text-gray-400">Síguenos:</span>
                        <div class="flex space-x-3">
                            <a href="https://facebook.com/AJ.Barriofino"
                               target="_blank"
                               rel="noopener noreferrer"
                               class="group w-10 h-10 bg-gray-800 hover:bg-blue-600 rounded-lg flex items-center justify-center transition-all duration-300 hover:scale-110 hover:shadow-lg">
                                <svg class="w-5 h-5 text-gray-300 group-hover:text-white transition-colors" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                                </svg>
                            </a>
                            <a href="https://instagram.com/barrioffinoo"
                               target="_blank"
                               rel="noopener noreferrer"
                               class="group w-10 h-10 bg-gray-800 hover:bg-gradient-to-tr hover:from-pink-500 hover:to-yellow-500 rounded-lg flex items-center justify-center transition-all duration-300 hover:scale-110 hover:shadow-lg">
                                <svg class="w-5 h-5 text-gray-300 group-hover:text-white transition-colors" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                                </svg>
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Enlaces rápidos -->
                <div>
                    <h3 class="font-semibold text-lg mb-6 text-white flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/>
                        </svg>
                        Enlaces Rápidos
                    </h3>
                    <div class="space-y-3">
                        <a href={`${import.meta.env.BASE_URL}`} class="flex items-center text-gray-300 hover:text-white transition-colors duration-300 text-sm group">
                            <svg class="w-4 h-4 mr-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            Inicio
                        </a>
                        <a href={`${import.meta.env.BASE_URL}historia`} class="flex items-center text-gray-300 hover:text-white transition-colors duration-300 text-sm group">
                            <svg class="w-4 h-4 mr-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            Historia
                        </a>
                        <a href={`${import.meta.env.BASE_URL}fotos-videos`} class="flex items-center text-gray-300 hover:text-white transition-colors duration-300 text-sm group">
                            <svg class="w-4 h-4 mr-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            Galería
                        </a>
                        <a href={`${import.meta.env.BASE_URL}eventos`} class="flex items-center text-gray-300 hover:text-white transition-colors duration-300 text-sm group">
                            <svg class="w-4 h-4 mr-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            Eventos
                        </a>
                        <a href={`${import.meta.env.BASE_URL}contacto`} class="flex items-center text-gray-300 hover:text-white transition-colors duration-300 text-sm group">
                            <svg class="w-4 h-4 mr-3 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            Contacto
                        </a>
                    </div>
                </div>

                <!-- Contacto -->
                <div>
                    <h3 class="font-semibold text-lg mb-6 text-white flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                        </svg>
                        Contacto
                    </h3>
                    <div class="space-y-4 text-sm">
                        <div class="flex items-start group">
                            <div class="w-8 h-8 bg-gray-800 rounded-lg flex items-center justify-center mr-3 flex-shrink-0 group-hover:bg-gray-700 transition-colors">
                                <svg class="w-4 h-4 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                                </svg>
                            </div>
                            <div>
                                <p class="text-gray-400 text-xs mb-1">Email</p>
                                <a href="mailto:aj.barrifino2023@gmail.com" class="text-gray-300 hover:text-white transition-colors break-all">
                                    aj.barrifino2023@gmail.com
                                </a>
                            </div>
                        </div>
                        <div class="flex items-start group">
                            <div class="w-8 h-8 bg-gray-800 rounded-lg flex items-center justify-center mr-3 flex-shrink-0 group-hover:bg-gray-700 transition-colors">
                                <svg class="w-4 h-4 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                                </svg>
                            </div>
                            <div>
                                <p class="text-gray-400 text-xs mb-1">Ubicación</p>
                                <p class="text-gray-300">C. Obispo Almarcha, 03317</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Bottom Footer -->
        <div class="border-t border-gray-700 py-6">
            <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
                <div class="text-center md:text-left">
                    <p class="text-sm text-gray-400">
                        © 2025 Peña BarrioFino. Todos los derechos reservados.
                    </p>
                </div>
                <div class="flex items-center space-x-6">
                    <a href="#" class="text-xs text-gray-400 hover:text-gray-300 transition-colors">Política de Privacidad</a>
                    <a href="#" class="text-xs text-gray-400 hover:text-gray-300 transition-colors">Términos de Uso</a>
                    <div class="flex items-center space-x-2 text-xs text-gray-500">
                        <span>Hecho con</span>
                        <svg class="w-3 h-3 text-red-500" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z"/>
                        </svg>
                        <span>en España</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</footer>

<!-- AOS JavaScript -->
<script src="https://unpkg.com/aos@2.3.4/dist/aos.js" is:global></script>
<script is:global>
document.addEventListener('DOMContentLoaded', () => {
    // Initialize AOS
            if (window.AOS) {
        window.AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true,
            offset: 50,
            disable: 'phone'
        });
    }

    // Mobile menu functionality
            const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuLines = document.querySelectorAll('.mobile-menu-line');
    let isMenuOpen = false;

    mobileMenuButton?.addEventListener('click', () => {
        isMenuOpen = !isMenuOpen;

        if (isMenuOpen) {
            // Open menu
                    mobileMenu.style.maxHeight = mobileMenu.scrollHeight + 'px';
            mobileMenu.classList.add('mobile-menu-open');

            // Animate hamburger to X
                    menuLines[0].style.transform = 'rotate(45deg) translate(6px, 6px)';
            menuLines[1].style.opacity = '0';
            menuLines[2].style.transform = 'rotate(-45deg) translate(6px, -6px)';

            // Add body scroll lock
                    document.body.style.overflow = 'hidden';
        } else {
            // Close menu
                    mobileMenu.style.maxHeight = '0';
            mobileMenu.classList.remove('mobile-menu-open');

            // Reset hamburger
                    menuLines[0].style.transform = 'none';
            menuLines[1].style.opacity = '1';
            menuLines[2].style.transform = 'none';

            // Remove body scroll lock
                    document.body.style.overflow = 'unset';
        }
    });

    // Close mobile menu when clicking on links
            const mobileMenuLinks = mobileMenu?.querySelectorAll('a');
    mobileMenuLinks?.forEach(link => {
        link.addEventListener('click', () => {
            isMenuOpen = false;
            mobileMenu.style.maxHeight = '0';
            mobileMenu.classList.remove('mobile-menu-open');

            // Reset hamburger
                    menuLines[0].style.transform = 'none';
            menuLines[1].style.opacity = '1';
            menuLines[2].style.transform = 'none';

            // Remove body scroll lock
                    document.body.style.overflow = 'unset';
        });
    });

    // Close mobile menu when clicking outside
            document.addEventListener('click', (e) => {
        if (isMenuOpen && !mobileMenuButton.contains(e.target) && !mobileMenu.contains(e.target)) {
            isMenuOpen = false;
            mobileMenu.style.maxHeight = '0';
            mobileMenu.classList.remove('mobile-menu-open');

            // Reset hamburger
                    menuLines[0].style.transform = 'none';
            menuLines[1].style.opacity = '1';
            menuLines[2].style.transform = 'none';

            // Remove body scroll lock
                    document.body.style.overflow = 'unset';
        }
    });

    // Handle window resize
            window.addEventListener('resize', () => {
        if (window.innerWidth >= 1024 && isMenuOpen) {
            isMenuOpen = false;
            mobileMenu.style.maxHeight = '0';
            mobileMenu.classList.remove('mobile-menu-open');

            // Reset hamburger
                    menuLines[0].style.transform = 'none';
            menuLines[1].style.opacity = '1';
            menuLines[2].style.transform = 'none';

            // Remove body scroll lock
                    document.body.style.overflow = 'unset';
        }
    });

    // Navbar scroll effect
            let lastScrollY = window.scrollY;
    const navbar = document.querySelector('nav');

    window.addEventListener('scroll', () => {
        const currentScrollY = window.scrollY;

        if (currentScrollY > 100) {
            navbar.classList.add('scrolled');
            if (currentScrollY > lastScrollY && currentScrollY > 200 && !isMenuOpen) {
                navbar.style.transform = 'translateY(-100%)';
            } else {
                navbar.style.transform = 'translateY(0)';
            }
        } else {
            navbar.classList.remove('scrolled');
            navbar.style.transform = 'translateY(0)';
        }

        lastScrollY = currentScrollY;
    });

    // Smooth scrolling for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
});
</script>
</body>
</html>

<style>
    .font-inter {
        font-family: 'Inter', sans-serif;
    }

    /* Navbar scroll effects */
    nav {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    nav.scrolled {
        background-color: rgba(255, 255, 255, 0.98);
        backdrop-filter: blur(20px);
        box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
    }

    /* Mobile menu improvements */
    #mobile-menu {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(249, 250, 251, 0.98) 100%);
        backdrop-filter: blur(20px);
        border-radius: 0 0 1rem 1rem;
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }

    #mobile-menu.mobile-menu-open {
        animation: slideDown 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }

    @keyframes slideDown {
        0% {
            opacity: 0;
            transform: translateY(-20px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Hamburger menu lines */
    .mobile-menu-line {
        transform-origin: center;
        will-change: transform, opacity;
    }

    /* Footer gradient animation */
    footer {
        background: linear-gradient(-45deg, #1f2937, #111827, #0f172a, #1f2937);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
    }

    @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    /* Custom scrollbar */
    ::-webkit-scrollbar {
        width: 8px;
    }

    ::-webkit-scrollbar-track {
        background: #f1f1f1;
    }

    ::-webkit-scrollbar-thumb {
        background: #888;
        border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
        background: #555;
    }

    /* Smooth transitions */
    * {
        transition-property: color, background-color, border-color, text-decoration-color, fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter;
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
        transition-duration: 300ms;
    }

    /* Focus styles for accessibility */
    a:focus,
    button:focus {
        outline: 2px solid #6b7280;
        outline-offset: 2px;
        border-radius: 0.375rem;
    }

    /* Loading state for images */
    img {
        loading: lazy;
    }

    /* Responsive typography */
    @media (max-width: 640px) {
        html {
            font-size: 14px;
        }
    }

    /* Prevent layout shift */
    .container {
        width: 100%;
        margin-left: auto;
        margin-right: auto;
        max-width: 1280px;
    }

    /* Enhanced hover effects */
    .hover-lift:hover {
        transform: translateY(-2px);
    }

    .hover-scale:hover {
        transform: scale(1.05);
    }

    /* Text selection */
    ::selection {
        background-color: #e5e7eb;
        color: #111827;
    }

    ::-moz-selection {
        background-color: #e5e7eb;
        color: #111827;
    }

    /* Print styles */
    @media print {
        nav, footer {
            display: none !important;
        }

        main {
            padding-top: 0 !important;
        }

        * {
            background: white !important;
            color: black !important;
        }
    }

    /* High contrast mode support */
    @media (prefers-contrast: high) {
        .bg-gray-50 {
            background-color: white;
        }

        .text-gray-600 {
            color: black;
        }

        .border-gray-200 {
            border-color: black;
        }
    }

    /* Reduced motion support */
    @media (prefers-reduced-motion: reduce) {
        *, ::before, ::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
            scroll-behavior: auto !important;
        }

        footer {
            animation: none !important;
        }
    }
</style>